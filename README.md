# contains the use of PCA in supervised & unsupervised learning to predict cancer datasets 
# K-means algorithm with cutting, different values of k, splitting of classes and with PCA to reduce variance
